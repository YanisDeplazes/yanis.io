---
const { url, title, target } = Astro.props;
---

<a class="link" href={url} target={target} aria-label={'explore ' + title}></a>

<style lang="scss">
  .link {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 2;
  }
</style>
