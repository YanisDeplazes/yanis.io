---
import Layout from '../../layouts/Layout.astro';
import Gallery from '../../components/sections/Gallery.astro';
import ContactSection from '../../components/sections/Contact.astro';
import { overview } from '../../data/gallery.json'; // Import the data

import Image from '../../assets/images/default.png';
---

<Layout
  title="Gallery  — Yanis Deplazes"
  description="A curated selection of my photography and visual exploration"
  imageSrc={Image.src}
>
  {
    overview.content ? (
      <Gallery galleryData={overview.content} title="Gallery - Overview" />
    ) : (
      <p class="no-images">No images available for this event.</p>
    )
  }
  <ContactSection />
</Layout>
